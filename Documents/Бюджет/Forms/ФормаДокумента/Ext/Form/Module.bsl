
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	ITC_ОбщиеСервер.ПроверитьЗаполнитьАвтора(Объект.Ответственный);
	//Если не РольДоступна("РП") тогда
	//	Отказ = Истина;
	//Иначе
	//	Отказ = Ложь;
	//КонецЕсли;
КонецПроцедуры

&НаКлиенте
Процедура СтатьиБюджетаЦенаЧасаПриИзменении(Элемент)
	СтатьиБюджетаЦенаЧасаПриИзмененииНаСервере();
КонецПроцедуры

&НаСервере
Процедура СтатьиБюджетаЦенаЧасаПриИзмененииНаСервере()
		
КонецПроцедуры

