
&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)

	ТекстовыйДокумент = Новый ТекстовыйДокумент;
	ТекстовыйДокумент.ДобавитьСтроку(ТекстОбАвторе());
	ТекстовыйДокумент.ТолькоПросмотр = Истина;
	ТекстовыйДокумент.Показать("О подсистеме и авторе");
	
КонецПроцедуры


&НаСервере
Функция ТекстОбАвторе()
	
	Ответ = Обработки.ТелеграмОбновлениеВерсии.ПолучитьМакет("ОПодсистемеИАвторе").ПолучитьТекст();
	Возврат Ответ;
	
КонецФункции
