// Шаблон вывода четырех кнопок — 2 в первой строке, 2 во второй строке
МассивРядовКнопок = Новый Массив;

ПервыйРядКнопок = Новый Массив;

Кнопка = Новый Структура;
Кнопка.Вставить("text", "Название первой кнопки");
// Опционально — запрос контакта или запрос местоположения (одно из двух)
// Кнопка.Вставить("request_contact", Истина);
// Кнопка.Вставить("request_location", Истина);
ПервыйРядКнопок.Добавить(Кнопка);

Кнопка = Новый Структура;
Кнопка.Вставить("text", "Название второй кнопки");
// Опционально — запрос контакта или запрос местоположения (одно из двух)
// Кнопка.Вставить("request_contact", Истина);
// Кнопка.Вставить("request_location", Истина);
ПервыйРядКнопок.Добавить(Кнопка);

МассивРядовКнопок.Добавить(ПервыйРядКнопок);

ВторойРядКнопок = Новый Массив;

Кнопка = Новый Структура;
Кнопка.Вставить("text", "Название третьей кнопки");
// Опционально — запрос контакта или запрос местоположения (одно из двух)
// Кнопка.Вставить("request_contact", Истина);
// Кнопка.Вставить("request_location", Истина);
ВторойРядКнопок.Добавить(Кнопка);

Кнопка = Новый Структура;
Кнопка.Вставить("text", "Название четвертой кнопки");
// Опционально — запрос контакта или запрос местоположения (одно из двух)
// Кнопка.Вставить("request_contact", Истина);
// Кнопка.Вставить("request_location", Истина);
ВторойРядКнопок.Добавить(Кнопка);

МассивРядовКнопок.Добавить(ВторойРядКнопок);

Клавиатура = Новый Структура;
Клавиатура.Вставить("keyboard", МассивРядовКнопок);
// Опционально — авторазмер
// Клавиатура.Вставить("resize_keyboard", Истина);
// Опционально — скрыть после нажатия (при этом она не удаляется)
// Клавиатура.Вставить("one_time_keyboard", Истина);