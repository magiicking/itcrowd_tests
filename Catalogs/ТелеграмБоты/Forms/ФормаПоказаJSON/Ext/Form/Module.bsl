
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	СтрокаJSON = Параметры.СтрокаJSON;
	
	Отформатированная = Параметры.Отформатированная;
	
	Если НЕ Отформатированная Тогда
		СтрокаJSON = ТелеграмСервер.ФорматироватьСтрокуJSON(СтрокаJSON);
	КонецЕсли;
	
	Если ЗначениеЗаполнено(Параметры.Заголовок) Тогда
		ЭтаФорма.Заголовок = Параметры.Заголовок;
	Иначе
		ЭтаФорма.Заголовок = "Ответ сервера Telegram";
	КонецЕсли;
	
КонецПроцедуры
