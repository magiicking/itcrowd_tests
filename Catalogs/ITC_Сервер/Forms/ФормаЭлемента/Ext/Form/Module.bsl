
&НаКлиенте
Асинх Процедура КаталогБазПоУмолчаниюНачалоВыбора(Элемент, ДанныеВыбора, СтандартнаяОбработка)
	ДВФ = Новый ДиалогВыбораФайла(РежимДиалогаВыбораФайла.ВыборКаталога);
	РезультатВыбора = Ждать ДВФ.ВыбратьАсинх();
	Если РезультатВыбора <> Неопределено Тогда
		Объект.КаталогБазПоУмолчанию = РезультатВыбора[0];	
	КонецЕсли;
КонецПроцедуры
