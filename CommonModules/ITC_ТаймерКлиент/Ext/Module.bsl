
#Если Клиент Тогда

Функция ЗапуститьТаймер(ъЗадача, ъФорма, ъВремя = Неопределено) Экспорт
	яО = ITC.Ответ_СформироватьСтруктуру();
	
	яВремя = ?(ъВремя = Неопределено, ТекущаяДата(), ъВремя);
	яПараметры = Новый Структура("ВремяЗапуска,Задача", яВремя, ъЗадача);
	ОткрытьФорму("Обработка.ITC_Таймер.Форма.ФормаЗапускаТаймера", яПараметры, ъФорма, Новый УникальныйИдентификатор,,,, РежимОткрытияОкнаФормы.БлокироватьОкноВладельца);
	
	Возврат яО;	
КонецФункции

#КонецЕсли